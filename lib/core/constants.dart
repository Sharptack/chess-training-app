class AppConstants {
  AppConstants._();

  static const appName = 'Chess Trainer';

  // Timeouts, storage keys, etc. (placeholders for Phase 0)
  static const httpTimeoutMs = 15000;

  // Routing helpers
  static const defaultCampaignId = 'beginner';
  static const defaultLevelId = '0001';
}